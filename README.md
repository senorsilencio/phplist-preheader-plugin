# phplist-preheader-plugin
A phplist plugin that adds a dedicated "Preheader" tab to the campaign editor, allowing editors to set a hidden preview text for email clients without touching the HTML template
